# Discord application (https://discord.com/developers/applications)
DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# Comma-separated Discord role IDs allowed to file issues. Leave empty to allow everyone.
ALLOWED_ROLE_IDS=

# Fine-grained GitHub PAT scoped to the target repo, Issues: Read & Write only
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=

# Comma-separated labels applied to every issue filed via Discord
DEFAULT_LABELS=from-discord

# Where the message-id -> issue-url dedupe map is persisted
STORE_PATH=./data/store.json

# Minimum seconds between issues filed by the same Discord user
COOLDOWN_SECONDS=10
