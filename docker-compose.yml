services:
  cypher:
    build: .
    image: cypher:local
    container_name: cypher
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - STORE_PATH=/app/data/store.json
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
